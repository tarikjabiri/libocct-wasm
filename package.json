{
  "type": "module",
  "scripts": {
    "setup": "node setup.ts",
    "build": "cmake --preset Release && cmake --build --preset Release",
    "zip": "zip -r libocct-wasm.zip ./libocct-wasm/"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}